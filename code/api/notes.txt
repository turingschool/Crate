Step One:
 - Create a PostGres DB (crate_test) $ psql
  - CREATE DATABASE <db_name>;

Step Two:
 - Modify `src/config/database.json`
  "test": {
    "username": "postgres",
    "password": null,
    "database": "crate_testing", # modify this to be different, so we don't teardown our development db when running tests!
    "host": "127.0.0.1",
    "dialect": "postgres",
    "seederStorage": "sequelize"
  },

Step Three:
 - $ NODE_ENV=test npm db:migrate; NODE_ENV=test npm db:seed

Step Four:
 - $ npm install --save-dev jest
 - $ npm install --save-dev supertest

Step Five:
 Modify package.json to add jest watcher to scripts

  "test:watch": "jest --watch"
